services:
  - type: web
    name: diario-emocional-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt && python init_db.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: ENVIRONMENT
        value: production
      - key: GEMINI_API_KEY
        sync: false # Debe ser configurado en Render dashboard
      - key: FRONTEND_URL
        sync: false # URL de tu frontend en Vercel